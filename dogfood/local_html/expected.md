# Expected Result

- Creates `artifacts\dogfood\local_html\local_dogfood.html`.
- Verifies mixed textbox, radio, checkbox, dropdown, textarea, and button controls.
- Writes a per-task JSON report.

SKIPPED is acceptable only when the local form semantics command or fixture parsing is unavailable.

