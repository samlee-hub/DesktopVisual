case_version=2
# VS Code dogfood is executed by run.ps1 because it needs environment discovery,
# an isolated user-data-dir, and process cleanup.

target_title="sample.txt"
observe
type text="VSCODE_DOGFOOD_APPENDED_TEXT"
hotkey keys="CTRL+S"
