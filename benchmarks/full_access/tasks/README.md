# Full Access Benchmark Tasks

`full_access_benchmark_matrix.ps1` generates runtime task JSON fixtures under `artifacts\benchmark\full_access\generated`. This directory is reserved for stable checked-in examples as the benchmark surface expands.
