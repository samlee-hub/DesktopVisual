# Installation

1. Clone or download this repository.
2. Open PowerShell in the repository root.
3. Verify the binary checksum.
4. Run `scripts\smoke-test.ps1`.
5. Use `bin\winagent.exe version` and `bin\winagent.exe help` to inspect the runtime.

The binary is unsigned. Review Windows security prompts before execution.