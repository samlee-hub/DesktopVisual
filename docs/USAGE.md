# Usage

Run commands from the repository root:

```powershell
.\bin\winagent.exe version
.\bin\winagent.exe help
.\bin\winagent.exe safety-report
```

For GUI input, provide an explicit target title and operate only on windows you are authorized to control. Stop on any error code and review the command output before continuing.